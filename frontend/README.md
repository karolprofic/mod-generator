# Frontend

React + TypeScript + Webpack.

## Uruchomienie (dev)

W katalogu `frontend`:

```bash
npm install
npm run dev
```

Aplikacja działa na `http://localhost:5173`.

## Build

```bash
npm run build
```

Wynik trafia do `frontend/dist`.
